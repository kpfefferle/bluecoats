Please upgrade the following dependency: $ARGUMENTS

Follow these steps:

1. Create a new Git branch for the dependency update
2. Upgrade the $ARGUMENTS dependency to the latest version
3. Ensure that the upgrade passes linting and type checking
4. Run the existing test suite to verify the upgrade
7. Commit the update to the branch with a descriptive commit message
8. Push the upgrade branch to GitHub and open a pull request against the main branch of the repository

Remember to use the GitHub CLI (`gh`) for all GitHub-related tasks.